openapi: 3.0.0
info:
  title: Converted from AppIdentifier.yaml with typeconv
  version: '1'
  x-id: AppIdentifier.yaml
  x-comment: >-
    Generated from src/main/resources/ts/api/AppIdentifier.ts by
    core-types-json-schema (https://github.com/grantila/core-types-json-schema)
    on behalf of typeconv (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    AppIdentifier:
      properties:
        appId:
          title: AppIdentifier.appId
          description: >-
            The unique application identifier located within a specific
            application directory instance. An example of an appId might be
            'app@sub.root'
          type: string
        instanceId:
          title: AppIdentifier.instanceId
          description: >-
            An optional instance identifier, indicating that this object
            represents a specific instance of the application described.
          type: string
        desktopAgent:
          title: AppIdentifier.desktopAgent
          description: >-
            The Desktop Agent that the app is available on. Used in Desktop
            Agent Bridging to identify the Desktop Agent to target.

            @experimental Introduced in FDC3 2.1 and may be refined by further
            changes outside the normal FDC3 versioning policy.
          type: string
      required:
        - appId
      additionalProperties: false
      title: AppIdentifier
      description: |-
        SPDX-License-Identifier: Apache-2.0
        Copyright FINOS FDC3 contributors - see NOTICE file
      type: object
