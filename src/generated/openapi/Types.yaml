openapi: 3.0.0
info:
  title: Converted from Types.yaml with typeconv
  version: '1'
  x-id: Types.yaml
  x-comment: >-
    Generated from src/main/resources/ts/api/Types.ts by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    ContextHandler:
      title: ContextHandler
      description: >-
        Describes a callback that handles a context event.

        Used when attaching listeners for context broadcasts.


        Optional metadata about the context message, including the app that
        originated

        the message, SHOULD be provided by the desktop agent implementation.
    IntentResult:
      anyOf: []
      title: IntentResult
      description: |-
        Intents can return results that are either context data objects
        or a reference to a Channel.
    IntentHandler:
      title: IntentHandler
      description: >-
        Describes a callback that handles a context event and may return a

        promise of a Context or Channel object to be returned to the

        application that raised the intent.

        Used when attaching listeners for raised intents.


        Optional metadata about the raised intent, including the app that
        originated

        the message, SHOULD be provided by the desktop agent implementation.
